{% extends 'base.html.twig' %}

{% block title %}TypePayment{% endblock %}

{% block body %}
    <h1>TypePayment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_payment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_payment.name }}</td>
            </tr>
            <tr>
                <th>Informations</th>
                <td>{{ type_payment.informations }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_payment_index') }}">back to list</a>

    <a href="{{ path('type_payment_edit', {'id': type_payment.id}) }}">edit</a>

    {{ include('type_payment/_delete_form.html.twig') }}
{% endblock %}
