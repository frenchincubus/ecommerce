{% extends 'base.html.twig' %}

{% block title %}CartProducts{% endblock %}

{% block body %}
    <h1>CartProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ cart_product.quantity }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ cart_product.date ? cart_product.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cart_products_index') }}">back to list</a>

    <a href="{{ path('cart_products_edit', {'id': cart_product.id}) }}">edit</a>

    {{ include('cart_products/_delete_form.html.twig') }}
{% endblock %}
