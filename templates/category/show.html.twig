{% extends 'admin/admin_home.html.twig' %}

{% block title %}Categorie {{ category.name }}{% endblock %}

{% block body %}
    <h1>Categorie {{ category.name }}</h1>
    <div class="row">
        <div class="col text-left" style="padding:10px;"><a href="{{ path('category_index') }}" class="btn btn-secondary" style="margin-right:10px;"><i class="material-icons align-bottom">keyboard_arrow_left</i>Retour à la liste</a></div>        
        <div class="col text-right" style="padding:10px;"><a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-warning">Modifier</a></div>       
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ category.url }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
