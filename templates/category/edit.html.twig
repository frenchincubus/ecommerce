{% extends 'admin/admin_home.html.twig' %}

{% block title %}Modifier Categorie{% endblock %}

{% block body %}
    <h1>Modifier Categorie</h1>

    <div>
         <a href="{{ path('category_index') }}" class="btn btn-secondary"><i class="material-icons align-bottom">keyboard_arrow_left</i>Retour à la liste</a>
    </div>

    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour' }) }}

    <div class="float-right">{{ include('category/_delete_form.html.twig') }}</div>
{% endblock %}
