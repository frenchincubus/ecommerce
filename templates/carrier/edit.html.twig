{% extends 'admin/admin_home.html.twig' %}

{% block title %}Modification transporteur{% endblock %}

{% block body %}
    <h1>Modification du transporteur "{{ carrier.name }}"</h1>
 <a href="{{ path('carrier_index') }}" class="btn btn-secondary"><i class="material-icons align-bottom">keyboard_arrow_left</i>Retour à la liste</a>
    {{ include('carrier/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

   

    {{ include('carrier/_delete_form.html.twig') }}
{% endblock %}
