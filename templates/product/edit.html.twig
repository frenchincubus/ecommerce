{% extends 'admin/admin_home.html.twig' %}

{% block title %}Modifier produit {{ product.name }}{% endblock %}

{% block body %}
    <h1>Modifier produit {{ product.name }}</h1>
    <div>
         <a href="{{ path('product_index') }}" class="btn btn-secondary"><i class="material-icons align-bottom">keyboard_arrow_left</i>Retour à la liste</a>
    </div>
    {{ include('product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

   
    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
