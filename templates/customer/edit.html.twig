{% extends 'admin/admin_home.html.twig' %}

{% block title %}Modifier client{% endblock %}

{% block body %}
    <h1>Modifier client</h1>
    <div>
        <a href="{{ path('customer_index') }}" class="btn btn-secondary"><i class="material-icons align-bottom">keyboard_arrow_left</i>Retour à la liste</a>
    </div>
    {{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
